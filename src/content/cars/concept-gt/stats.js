/**
 * Concept GT: A heavy, high-speed hypercar with powerful acceleration
 * and strong straight-line speed, but slightly less maneuverable.
 *
 * @type {import('../_template/stats.js').CarStats}
 */
const stats = {
  maxSpeed:    50, // fast
  maxReverse:  12,
  accel:       28, // strong acceleration
  reverseAccel: 15,
  brake:       33,
  drag:        4.5,
  maxTurn:     2.5, // slightly wider turning radius
  gripNormal:  7.2, // slightly slippier
  gripDrift:   1.6,
};

export default stats;
