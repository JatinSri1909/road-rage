/**
 * Implement `buildScenery` to add decorations (trees, grandstands,
 * billboards, mountains) that are unique to your track. Call this from
 * your track.js or register it as an export for track-builder to pick up.
 *
 * @param {import('three').Scene} scene
 * @param {Object} options
 * @param {import('three').Vector3[]} options.samplePts
 * @param {import('three').Vector3[]} options.sampleTangents
 * @param {number} options.ROAD_W
 * @param {import('three').Vector3} options.UP
 * @param {Array} options.colliders
 */
export function buildScenery(scene, { samplePts, sampleTangents, ROAD_W, UP, colliders }) {
  // TODO: add scenery specific to your track here
  // Example:
  //   import { buildTrees } from '../../../entities/track-builder.js';
  //   buildTrees(scene, samplePts, sampleTangents, ROAD_W, colliders);
}
